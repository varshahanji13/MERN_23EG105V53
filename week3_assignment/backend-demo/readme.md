#STEPS TO CREATE A BACKEND

**1.Generate package.json**
 npm init -y --> node program manager initalize -yes means accepting 

**2.Create HTTP Server** 
  
  **a.Install and import 'express' module**
   npm install          --> to install any module
   npm install express  -->it checks for node_modules if not present it will create that package/folder

  **b.import express module (create server.js)**
   
  **c.create HTTP servers**





**HTTP request types (CRUD operations)**

GET - Read resources 
PUT - update the resouce
POST - createa new resource
DELETE - delete a resoure


**#USER API:**

http://127.0.0.1:3000--> local server address-->3000 iis the port number for identifying the specfic application in local server

http://localhost:3000-->localhost is the domain name for above 
                      
http://localhost:3000/path --> add path of the application

GET    http://localhost:3000/path    //making get request
POST   http://localhost:3000/path 
PUT    http://localhost:3000/path/<id>
DELETE http://localhost:3000/path/<id>

